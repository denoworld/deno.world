{
  "scripts": {
    "build": "npm run build:css",
    "build:css": "NODE_ENV=production postcss global.css -o public/style.css --verbose"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.4",
    "cssnano": "^4.1.10",
    "esbuild": "^0.8.42",
    "postcss": "^8.2.4",
    "postcss-cli": "^8.3.1",
    "tailwindcss": "^2.0.2"
  }
}
